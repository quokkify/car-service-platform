# Latest test pyramid snapshot

The generated snapshot is [`pyramid-snapshot.md`](./pyramid-snapshot.md). Machine-readable outputs are [`pyramid-snapshot.json`](./pyramid-snapshot.json) and [`pyramid-quality-gates.json`](./pyramid-quality-gates.json).

This navigation page is kept stable; the scheduled **Test Pyramid Snapshot Refresh** updates the named snapshot files only.
